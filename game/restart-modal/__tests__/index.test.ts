import {useRestartModalController} from '../controller';

describe('Restart Modal', () => {
  it('should not show highscore is gameOverReason in paused', () => {
    // const controller = useRestartModalController({});
  });

  it('should show resume button along with other buttons if  gameOverReason in paused', () => {
    // const controller = useRestartModalController({});
  });

  it('should show restart, quit button along with  buttons if  gameOverReason is timeOut or wrongAnswer', () => {
    // const controller = useRestartModalController({});
  });

  it('should pause game on back button press', () => {
    // const controller = useRestartModalController({});
  });

  it('should resume game on resume option click', () => {
    // const controller = useRestartModalController({});
  });

  it('should quit game on quit button click', () => {
    // const controller = useRestartModalController({});
  });
});
